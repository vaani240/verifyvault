const Footer = () => {
  return (
    <footer className="vv-footer">
      <small>© {new Date().getFullYear()} VerifyVault. Document authenticity and verification made simple.</small>
    </footer>
  );
};

export default Footer;
